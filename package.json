{
  "name": "lud16-finder",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=browser && cp index.html dist/",
    "serve": "bun run build && bunx serve dist"
  },
  "dependencies": {
    "applesauce-core": "^5.0.0",
    "applesauce-relay": "^5.0.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
